// DALL-E промпти для генерації тексту

export const openaiTextPrompt = (formData) => {
  return `Сформуй 5 коротких текстів привітання українською мовою для ${formData.person} ${formData.age} років, ${formData.gender === 'Ч' ? 'чоловічої' : 'жіночої'} статі.

Контекст привітання: ${formData.greetingSubject || 'загальне привітання'}.

Хобі та захоплення: ${formData.hobbies.join(', ')}.

Риси та цінності: ${formData.traits.join(', ')}.

Настрій: ${formData.cardMood}.

Тексти мають бути:
- Персоналізованими та теплими
- Відповідати нарою та стилю
- Враховувати хобі та риси особистості
- Різноманітними за довжиною та стилем
- Підходящими для листівки або картки`;
}; 